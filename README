### 编译器课设

尽可能靠近scheme的一门语言.

特性:

#### 表达式
如486是一个表达式.
#### 组合式
形如 (+ 137 349), 用括号将一些表达式括起来, 形成一个表, 表示一个过程应用.
表最左元素称为运算符, 其他元素称为运算对象 (前缀表示).

基本运算符: +-*/ <>=

*暂时只支持二元组合式*

#### 定义
通过 **定义** 给事物命名(即变量).
(define size 2) 将值2与size关联.

`(define x y)` 即 `x=y`

定义求值结果为`x`
